### Minimal env for single-container, insecure internal deployment ###

# Django
DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=True
# Allow all hosts in internal use; you can set to your LAN IP/hostname
DJANGO_ALLOWED_HOSTS=*

# App data location (inside container); your host path is bind-mounted to this in compose
# Extracted frames (before curation)
KWC_EXTRACT_FOLDER=/media/extracted
# Curated wallpapers (after choosing)
KWC_WALLPAPERS_FOLDER=/media/wallpapers

# Optional naming templates
KWC_FOLDER_PATTERN={{ title }}{% if year %} ({{ year }}){% endif %}
KWC_IMAGE_PATTERN={{ title }}{% if season %} S{{ season|pad:2 }}{% endif %}{% if episode %}{% if not season %} {% endif %}E{{ episode|pad:2 }}{% endif %} 〜 {{ counter|pad:4 }}.jpg
# Optional: cap extractor parallelism (defaults to container CPU count)
KWC_EXTRACT_WORKERS=
# Optional: default start path for file picker (defaults to /)
KWC_FILE_PICKER_START_PATH=/

# TMDB API key for cover art selection (optional)
# Get your API key from https://www.themoviedb.org/settings/api
TMDB_API_KEY=

# Server port inside the container
PORT=8000

# Optional: set these so the container runs as your host user (prevents root-owned files)
# On Linux, get your values with: id -u and id -g
KWC_UID=1000
KWC_GID=1000

# Copy this to .env and adjust if needed
